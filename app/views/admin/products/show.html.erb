<h1>商品　詳細画面</h1>

<%= attachment_image_tag @product, :image, :fill, 100, 100, format: "jpeg" %>
<p>商品名 <%= @product.name %></p>
<p>商品説明 <%= @product.description %></p>
<p>ジャンル <%= @product.genre.name %></p>
<p>税込価格(税抜価格) <%= @product.price * 1.1 .round %>(<%= @product.price %>)</p>
<p>販売ステータス
	<% if @product.is_soldout == true %>売切れ
		<% else %>
		販売中
	<% end %>
</p>

<%= link_to "編集する", edit_admin_product_path(@product), class:"btn btn-success btn-xs" %>
