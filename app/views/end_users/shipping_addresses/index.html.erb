<h2>配送先登録/一覧</h2>

<%= form_for(@shipping_address) do |address| %>

<h4>郵便番号(ハイフンなし)</h4><%= f.text_field :zip_code %>

<h4>住所</h4><%= f.text_area :address %>

<h4>宛名</h4><%= f.text_area :name %>　<%= f.submit "登録する" %>

<% end %>

